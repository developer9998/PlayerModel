using UnityEngine;

namespace PlayerModel.Models
{
    public enum EDigitType
    {
        [Tooltip("Index finger (trigger)")]
        Index,
        [Tooltip("Ring finger (grip)")]
        Ring,
        [Tooltip("Thumb (A/B/X/Y)")]
        Thumb
    }
}
